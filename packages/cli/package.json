{
  "name": "@pulsemcp/air-cli",
  "version": "0.7.0",
  "description": "CLI for AIR — validate, list, and inspect agent infrastructure config",
  "bin": {
    "air": "./dist/index.js"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "schemas"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node ../../scripts/copy-schemas.mjs",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@pulsemcp/air-core": "0.7.0",
    "@pulsemcp/air-sdk": "0.7.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
